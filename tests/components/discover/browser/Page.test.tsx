import { render, screen } from '@testing-library/react';
import Page from '@/app/discover/browser/page';
import * as React from 'react';

describe('Page', () => {
    it('should render correctly on intial load', () => {
        
    })
})